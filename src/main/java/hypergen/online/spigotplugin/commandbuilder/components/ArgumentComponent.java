package hypergen.online.spigotplugin.commandbuilder.components;

public class ArgumentComponent {

    public ArgumentComponent(LabelComponent labels, ErrorComponent error, ExecuteComponent execute, WhenComponent[] whens) {
    }

    public ArgumentComponent(LabelComponent labels, ErrorComponent error, WhenComponent[] whens) {
    }
}
