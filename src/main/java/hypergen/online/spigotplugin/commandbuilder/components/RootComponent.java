package hypergen.online.spigotplugin.commandbuilder.components;

public class RootComponent {
    public RootComponent(CommandsComponent commands, ErrorComponent error, ArgumentComponent argument) {
    }

    public RootComponent(CommandsComponent commands, ErrorComponent error, ExecuteComponent execute, ArgumentComponent argument) {
    }
}
